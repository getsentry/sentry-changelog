# Non-secret dev defaults, auto-loaded by `next dev`.
# Put secrets — your Neon dev-branch connection string and (optionally) the
# Vercel Blob token — in .env.development.local (gitignored). See the README.

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=secret
